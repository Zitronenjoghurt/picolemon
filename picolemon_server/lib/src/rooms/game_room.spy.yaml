class: GameRoom
table: game_room
fields:
  code: String
  gameType: String
  # ToDo: GameState
  hostUserId: UuidValue?
  createdAt: DateTime
  isActive: bool, default=true
indexes:
  game_room_code_idx:
    fields: code
    unique: true